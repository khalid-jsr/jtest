{
  "TransactionType": 2,
  "Approved" : "${approvalStatus}",
  "TransactionIdentifier" : "${txnID}",
  "IsoResponseCode" : "${IsoResponseCode}",
  "ResponseMessage" : "${ResponseMessage}",
  "OrderIdentifier" : "${OrderID}}",
  "RedirectData" : "${RedirectHTML}",
  "SpiToken" : "${SpiToken}"
}